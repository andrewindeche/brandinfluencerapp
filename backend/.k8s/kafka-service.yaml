apiVersion: v1
kind: Service
metadata:
  name: kafka-external
  labels:
    app: kafka
spec:
  type: LoadBalancer
  ports:
    - port: 9094
      targetPort: 9092
      protocol: TCP
      name: kafka-external
  selector:
    app: kafka